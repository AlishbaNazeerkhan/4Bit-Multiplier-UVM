import uvm_pkg::*;
import mul_pkg::*;
module tb_top;
//declare interface
mul_if mif();

//instatiate DUT

mul dut(
.a(mif.a),
.b(mif.b),
.y(mif.y)
);

initial begin
uvm_config_db #(virtual mul_if)::set(null,"","mif", mif); 
run_test("test");
end
/*
initial begin
$dumpfile("dump.vcd");
$dumpvars;
end
*/
endmodule
